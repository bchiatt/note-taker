.row
  .small-12.columns
    h3 Notebook
    button(ng-click='addNote()') Add Note
.row
  .small-12.columns
    table
      thead
        th Remove
        th Title
        th Updated
        th Tags
      tbody
        tr(ng-repeat='n in notes')
          td: a(href='', ng-click='remove(n.noteId, $index)') delete
          td: a(href='#/notes/{{n.noteId}}') {{n.title}}
          td {{n.updatedAt | date}}
          td
            span(ng-repeat='t in n.tags'): a(href='') {{t}}  
